package com.oversoul.repository;

import com.oversoul.entity.OnlineCourses;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OnlineCoursesRepository extends JpaRepository<OnlineCourses, Long>{
}
